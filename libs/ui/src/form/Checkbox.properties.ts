import { ControlWrapperProperties } from './core/ControlWrapper.properties';

export const CheckboxProperties = {
  ...ControlWrapperProperties,
};

export const CheckboxEmits = ['change', 'focus', 'blur'];
